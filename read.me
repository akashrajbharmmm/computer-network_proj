# 🌐 DNS Lookup Tool (C++ + Flask + Frontend)

A simple **DNS Lookup Tool** built using **C++ (Winsock API)** for backend logic,  
and **Flask (Python)** for the web interface.  
It allows you to:
- Resolve a **domain name → IP address**.
- Perform **reverse lookup (IP → domain name)**.

---

## 🧰 Features

✅ Domain to IP lookup (IPv4 / IPv6 supported)  
✅ Reverse DNS lookup (IP → Domain)  
✅ Modern, dark-themed web frontend  
✅ Works locally on Windows using Flask  

---

## 📂 Project Structure

